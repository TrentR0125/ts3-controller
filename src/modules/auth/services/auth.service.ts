import { Injectable } from "@nestjs/common";

// services will only be used to little things, like stuff that you think wouldnt need to be its own command or query (if that doesnt make sense i can try to elaborate more)

@Injectable()
export class AuthService {

}